import { StyleSheet } from "react-native";

const styles = {
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    },
    map:{
        flex: 1,
        height: '100%',
        width: '100%',
      }
}

export default styles;